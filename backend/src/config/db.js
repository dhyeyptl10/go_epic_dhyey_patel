// ============================================
// DB.JS – Database Config Placeholder
// Go-Epic uses JSON data files (no DB required)
// ============================================

const connectDB = () => {
  console.log('📦 Go-Epic is using in-memory JSON data store.');
  console.log('   No external database required.\n');
};

module.exports = connectDB;
